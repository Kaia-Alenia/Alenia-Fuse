[project]
name = "alenia-porter"
version = "7.0.0"
description = "Python code transformation and analysis tool"
readme = "README.md"
authors = [
    { name = "Alejandro Martinez", email = "contact.aleniastudios@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pillow>=10.0",
    "rich>=13.7.0",
    "prompt_toolkit>=3.0.40"
]

[project.scripts]
porter = "alenia_porter.cli.main:main"

[build-system]
requires = ["uv_build>=0.11.17,<0.12.0"]
build-backend = "uv_build"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["alenia_porter"]

[tool.setuptools.package-data]
alenia_porter = ["py.typed", "assets/images/*", "assets/locales/*", "assets/themes/*"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-mock>=3.15.1",
]
